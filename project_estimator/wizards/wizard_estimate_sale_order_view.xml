<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="view_wizard_estimate_sale_order_form" model="ir.ui.view">
            <field name="name">wizard.estimate.sale.order.form</field>
            <field name="model">wizard.estimate.sale.order</field>
            <field name="arch" type="xml">
                <form string="Create Sale Order">
                    <group>
                        <field name="pricelist_id"/>
                    </group>
                    <footer>
                        <button name="create_sale_order" string="Create Sale Order" type="object" class="btn-primary"/>
                        <button string="Cancel" class="btn-secondary" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>

        <record id="action_wizard_estimate_sale_order" model="ir.actions.act_window">
            <field name="name">Create Sale Order</field>
            <field name="res_model">wizard.estimate.sale.order</field>
            <field name="view_mode">form</field>
            <field name="target">new</field>
        </record>
    </data>
</odoo>
